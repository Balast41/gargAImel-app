{
  "name": "gargaimel-app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "seed:markdown": "electron seed-markdown-test.js",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up",
    "docker:seed": "docker compose run --rm gargaimel seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron": "^41.7.1"
  },
  "dependencies": {
    "better-sqlite3-multiple-ciphers": "^12.10.0",
    "dompurify": "^3.2.6",
    "marked": "^18.0.5"
  }
}
